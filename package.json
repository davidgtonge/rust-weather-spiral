{
  "name": "rust-weather-spiral",
  "version": "0.1.0",
  "description": "Weather spiral demo — Rust/Wasm engine, Open-Meteo data, canvas blit",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "license": "MIT",
  "dependencies": {
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
